function GridTiles(){
    let column = Math.floor(document.body.clientWidth / 50); 
    let row = Math.floor(document.body.clientHeight / 50); 

    const createTitles = (index) => {
        return(
            <div className="tile"></div>
        )
    }
    
}

export default GridTiles